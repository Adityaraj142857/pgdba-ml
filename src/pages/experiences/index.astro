---
import { getLatestBatch } from "../../lib/batches";
import { withBase } from "../../lib/url";

const latest = await getLatestBatch();
return Astro.redirect(withBase(`/experiences/${latest.data.batch}/`));
---
